# Copilot Instructions

## Project
- Use Python.
- Do not supress error from ruff.

## Virtual Environment
- Virtual environment is in 'venv' directory.
- If virtual environment does not exist, create it with `dev/windows/app_setup.ps1`.
